# JWT Secret for authentication (change this to something secure)
JWT_SECRET=your-super-secret-key-change-me

# Server port
PORT=5000

# Database path (optional, defaults to project root)
# DATABASE_PATH=./tasks.db
